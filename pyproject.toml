[project]
name = "sophiarch-notebooks"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.2",
    "pytest",
    "scikit-learn>=1.8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
